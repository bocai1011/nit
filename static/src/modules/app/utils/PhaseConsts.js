/**
 * Collection of string references for staging phases the application.
 * Use these references rather than hardcoded strings
 */
const PHASES = {
       REGISTRANTFILES : 'RegistrantFiles',
       CREATEDB: 'CreateDB',
       INTERPRETATION : 'Interpretation',
       REFERENCEDATA : 'ReferenceData',
       RECTIFICATION : 'Rectification',
       FINALIZE : 'Finalize',
       EXPORT : 'Export',
       SHARE : 'Share',
};

export default PHASES;
